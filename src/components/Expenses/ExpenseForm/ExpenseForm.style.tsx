import styled from 'styled-components';

export const ExpenseFormContainer = styled.form`
    ${({ theme }) => {
        const { colors } = theme;

        return ` 
            
        `;
    }}
`;
